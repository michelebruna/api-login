// Exemplo mais simples - apenas o que você precisa
const loginData = require('./fixtures/postLogin.json');
const username = loginData.username;

console.log('Username extraído:', username); // Output: Username extraído: grupo4 